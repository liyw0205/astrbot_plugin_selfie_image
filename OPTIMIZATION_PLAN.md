# Selfie Image 前端优化计划

> 版本：2026-09-13。本文以当前仓库和真实浏览器实测为准，不把旧实验代码、历史截图或“已经规划”当成已完成能力。
>
> 本轮只重构前端工作流和验收标准；后端继续复用现有会话、画布、模板、任务、记录、媒体缓存、权限和额度链路。未通过真实浏览器验收的项目不得标记为 done。

## 1. 范围与原则

### 1.1 页面职责

| 页面 | 负责的事情 | 不应承担的事情 |
| --- | --- | --- |
| 工作台 | 查看服务状态、进入常用入口 | 不重复承载试画和画布表单 |
| 快速试画 | 一次性验证提示词、模板、预设、COS 池和出图设置 | 不保存画布节点、不显示关系图 |
| 无限画布 | 选择独立会话、查看横向关系图、选择节点、创建子节点、重画/删除 | 不放完整参数表和图库长列表 |
| 创作画布 | 管理独立画布、模板参数、参考图槽位和后台生成 | 不读取无限画布会话，不复用其节点关系 |
| 预设管理 | 管理生图预设、视频预设和 COS 池 | 不在此执行生图 |
| 生成记录/资产库 | 筛选、查看、整理已有结果 | 不反向修改画布结构 |

### 1.2 交互不变量

- 无限画布和创作画布使用不同的会话命名空间；切换页面、刷新和重新登录都不能互相混入。
- 预设和 COS 池是共享只读缓存，页面只负责打开选择器；选择器关闭、切换和刷新不得清空另一个页面的输入。
- 一个页面只保留一个主要动作：快速试画是“开始试画”，无限画布节点是“生成/重新生成”，创作画布是“保存后生成”。禁用条件必须与按钮文案一致。
- 节点详情在关系图下方（移动端）或右侧面板（桌面端）；不能把完整表单塞进节点卡片，也不能用文字列表替代关系图。
- 图库按当前槽位打开，选中并确认后立即关闭；图库、预设、COS 池和生成结果不得同时覆盖同一块可视区域。
- 所有异步按钮必须有 `idle/loading/success/error` 四种可见状态，加载期间防止重复提交并保留取消或重试入口。

## 2. 实测基线

### 2.1 测试环境

- 页面：`http://43.138.178.130:14514/`，使用真实 Web Token 登录。
- 浏览器：Termux Chromium 149，Playwright Core 驱动真实页面；不是接口模拟或 Python 假页面。
- 桌面视口：`1440x900`；移动视口：`390x844`。
- 实测日期：2026-09-13。
- 覆盖页面：工作台、无限画布、创作画布、预设管理、快速试画、生成记录、资产库。

### 2.2 已验证正常的行为

- 登录后页面、侧栏和工作台可加载；桌面端无横向滚动溢出。
- 移动端汉堡菜单可以打开侧栏，点击菜单项后侧栏自动收起。
- 无限画布可以读取会话，当前测试会话显示 6 个节点、5 条边；滚轮缩放会改变画布缩放比例。
- 点击无限画布节点会高亮相邻边并打开详情；移动端详情位于画布下方，符合移动端阅读顺序。
- 创作画布和无限画布的会话下拉框彼此隔离。
- 快速试画在移动端会将表单单列布局，预设和 COS 池按钮可以打开，未发现页面级横向溢出。

### 2.3 实测发现的问题

| 优先级 | 证据 | 影响 |
| --- | --- | --- |
| P0 | 页面初始化阶段先请求一次未带 Token 的 `/api/config`，浏览器控制台出现 `401 GET /api/config`，之后才成功重试 | 用户看到“有错误”的控制台；嵌入页面或弱网下可能把首次失败误判为功能失败 |
| P0 | 创作画布图库打开后先请求 `records?limit=20&offset=0`，随后串行请求 `offset=20`；数秒内仍显示“加载图片…” | 图库不能即时使用，多个槽位重复打开会放大等待和请求量 |
| P0 | 当前 COS 池返回约 401 条，选择器一次性渲染全部条目和收藏按钮 | 手机端列表过长、首屏卡顿、滚动定位困难；选择器与输入区争夺屏幕空间 |
| P1 | 无限画布会恢复会话保存的 `35%~39%` 缩放；移动端 319px 画布中节点缩成很小的卡片，标题不可读 | 用户进入关系图后首先看到“看不清”的画布，需要手动点缩放按钮 |
| P1 | 移动端隐藏关系图左侧节点列表和搜索，只能点击缩小后的画布节点 | 节点多或节点在视口外时难以选择；搜索功能在手机端等于不可用 |
| P1 | 缺失媒体会请求 `/api/cache-image` 并返回多个 404，节点退化为“媒体已清理/生成结果” | 页面有控制台错误，节点身份和结果状态不够明确 |
| P1 | 创作画布“合影”会渲染 8 个槽位，其中多个重复为“额外参考”；移动端页面被拉到约 1800px | 槽位语义不清，用户要滚动很久才能生成，增加误填和错放参考图的风险 |
| P1 | 预设/COS 选择器状态互斥，但切换依赖异步加载；快速试画中 COS 面板会渲染数百个按钮 | 点击后短时间没有明显反馈，用户容易重复点击；切换期间的输入焦点和滚动位置不稳定 |
| P1 | 创作画布在没有结果时同时显示“生成图片”和“再次生成”，两者都可用 | 首次生成和重跑语义冲突，用户无法判断是否会复用旧结果 |
| P2 | 无限画布会话下拉框出现相同标题的重复会话（如多条“CDP关系网检查”） | 无法仅凭标题判断要进入哪个会话，删除/重画容易选错对象 |
| P2 | 节点卡片标题大量回退为“生成结果”，媒体失效时只剩通用状态 | 关系图失去上下文，分支比较和定位成本高 |

## 3. 前端状态边界

### 3.1 页面状态

```text
APP
├─ auth: logged_out | authenticating | authenticated | auth_error
├─ page: base | studio | creativeCanvas | presets | test | monitor | assets | ...
├─ pickerCache
│  ├─ imagePresets: idle | loading | ready | error
│  ├─ videoPresets: idle | loading | ready | error
│  └─ cosPool: idle | loading | ready | error
├─ studio
│  ├─ sessionId, graph, viewport, selectedNodeId
│  ├─ nodeTask: idle | queued | running | succeeded | failed | cancelled
│  └─ picker: closed | presets | cos
└─ creativeCanvas
   ├─ sessionId, slots, gallerySlotId
   ├─ task: idle | queued | running | succeeded | failed | cancelled
   └─ picker: closed | presets | cos | gallery
```

规则：页面切换只改变 `page`，不能销毁全局缓存；会话切换必须先停止旧任务轮询，再加载新会话；任何请求都要带上请求序号或会话 ID，旧响应不得覆盖新页面。

### 3.2 选择器约束

- 预设和 COS 池采用同一选择器组件：顶部搜索、数量摘要、虚拟列表或分页、空状态、加载状态和错误重试。
- 打开一个选择器前关闭同一输入上的另一个选择器；切换按钮立即更新为“加载中”，不得等接口完成后才改变视觉状态。
- 选择条目后只写入目标输入框，不触发保存或生图；确认生图由页面主按钮完成。
- COS 池首屏最多渲染 30 条，其余通过搜索、系列筛选或分页加载；不把 401 条目一次性插入 DOM。

### 3.3 媒体与图库约束

- 图库只加载当前槽位需要的记录，先显示分页骨架和文字元数据，缩略图按视口懒加载。
- 同一页面和两个画布共用图库缓存，缓存键包含 `kind/filter/page`；请求进行中复用 Promise，禁止每个槽位各发一遍首屏请求。
- 媒体 404 不应成为未处理的浏览器错误：节点显示“原图已清理，可重画/删除”，并停止重复重试。
- 选择结果后立即关闭图库并回到当前槽位；生成结果查看使用统一图片放大层，下载和关闭按钮固定在右上角。

## 4. 改造路线

### Phase 0：观测和契约（P0）

- 统一 `api()` 的认证启动顺序：先完成 token/嵌入 bridge，再发受保护请求；401 只在真正鉴权失败时进入错误态。
- 为页面请求记录 `page、requestKey、sessionId、startedAt、finishedAt、status`，不记录 Token、Cookie、完整提示词或原图。
- 给选择器、图库、画布任务增加可测量的加载状态和耗时字段。
- 给会话摘要返回稳定短标题、模板、更新时间和唯一短 ID，前端显示“标题 · ID 后四位”避免同名歧义。

验收：首次进入没有预期外 401；切换页面不出现旧请求覆盖新页面；重复点击只产生一个进行中的请求。

### Phase 1：共享选择器和图库（P0）

- 把快速试画、无限画布节点、创作画布共用的预设/COS 读取收敛到一个缓存服务和一个选择器组件。
- COS 池加入搜索、系列筛选、收藏筛选和虚拟列表；选择器固定高度，不能把主表单顶出视口。
- 图库改为当前槽位下方或侧边抽屉显示，分页/懒加载缩略图；“确定”写入槽位后自动关闭。
- 槽位由模板定义：合影只显示自己、同框对象和场景/道具，额外参考默认隐藏，通过“添加参考图”按需增加。

验收：首次打开预设/COS 在 1 秒内显示可操作骨架；缓存命中不重复请求；图库先可选元数据，首批缩略图到达后可选；选择后自动关闭；两个画布槽位数量与模板一致。

### Phase 2：无限画布可读性和节点操作（P1）

- 首次进入或 viewport 无效时按节点包围盒自动 fit；保存的缩放低于移动端可读阈值时自动提升到最小可读比例。
- 节点保持媒体缩略图、短标题、状态和时间；标题为空时使用模板名和节点序号，不再统一显示“生成结果”。
- 移动端保留“节点列表/搜索”抽屉入口；点击节点、搜索结果和画布节点使用同一个 selectedNode 状态。
- 画布支持空白拖拽、鼠标滚轮、双指缩放、节点拖动；缩放和平移不触发节点选择，节点操作按钮不触发画布拖动。
- 移动端详情固定排在画布后面；桌面端详情占右栏，打开详情时画布宽度和连线重新计算。

验收：390px 宽度下节点标题可读；从搜索结果可打开任意节点；横向父子关系和上下分支不被面板遮挡；刷新后 viewport 和 selectedNode 不产生跳变。

### Phase 3：生成动作和按钮语义（P1）

- 无限画布：草稿节点只显示“生成图片”，已有结果显示“重新生成”；根节点不可误删，删除和重画必须确认。
- 创作画布：没有成功结果时禁用“再次生成”；生成中只显示进度、取消和重新查询；成功后才显示“再次生成”。
- 快速试画：开始试画、查看请求、查看响应、查看结果按状态启用；模板预览和随机模板只修改输入，不自动提交任务。
- 所有生成动作复用现有任务轮询、额度、审核、provider 回退、记录来源和缓存，刷新页面后可恢复任务状态。

验收：重复点击不产生重复任务；失败状态有明确错误和重试；后台生成期间切换页面不丢任务；记录来源明确区分 `Web/画布名`、`Web/快速试画` 和 `Web/快速试视频`。

### Phase 4：响应式和视觉收敛（P1）

- 桌面端以“侧栏 + 主工作区 + 节点详情”组织，不在一个页面同时展开多个大型卡片。
- 移动端只保留汉堡导航；页面内部使用单列表单、可折叠高级区和固定底部/局部操作栏，主按钮不被浮动加号遮挡。
- 统一按钮尺寸和语义：刷新/删除/保存/生成采用一致的 mini、secondary、danger、ok 样式；图标按钮提供 `aria-label` 和 tooltip。
- 统一图片放大层、下载、关闭和长按/右键行为；缩略图不承担编辑和删除两个相互冲突的点击区域。

验收：`1440x900` 与 `390x844` 均无横向溢出；表单标签、控件和错误文案不重叠；浮动按钮不覆盖输入或主动作；键盘焦点顺序与视觉顺序一致。

## 5. 实测回归矩阵

| 场景 | 桌面端 | 移动端 | 通过标准 |
| --- | --- | --- | --- |
| 登录/首屏 | 登录、配置、健康状态 | 汉堡菜单、登录后首屏 | 无预期 401、无未处理 console error |
| 页面切换 | 工作台→快速试画→两种画布 | 打开菜单→选择页面→菜单自动关闭 | 页面状态和缓存不互相污染 |
| 会话选择 | 选择、刷新、同名区分 | 下拉可选、返回后仍在当前会话 | 两个画布会话隔离 |
| 无限画布 | 拖拽、滚轮、双击、节点详情 | 双指、点击节点、详情在图下 | 节点可读、边正确、无误触 |
| 节点生成 | 生成、重画、删除、取消 | 进度、取消、详情滚动 | 幂等、可恢复、按钮状态正确 |
| 模板选择 | 预设/COS 切换、搜索、写入提示词 | 面板不遮住主输入 | 共享缓存、互斥、可重试 |
| 创作画布 | 槽位、图库、保存、生成 | 槽位数量少且语义清晰 | 选择后图库关闭，结果更新 |
| 快速试画 | 模板预览、随机模板、批次变化 | 单列表单、主按钮可见 | 只修改输入，不意外提交 |
| 生成记录/资产库 | 筛选、图片放大、下载 | 图片不超出容器、操作可达 | 404 媒体可解释，不循环重试 |

## 6. 浏览器验收脚本

真实浏览器验收必须保留以下证据：

1. 使用 Termux Chromium 打开实际 Web 页面并登录，不以 `curl` 或 API 结果代替页面操作。
2. 分别使用 `1440x900` 和 `390x844`；记录页面、操作、预期、实际、耗时和截图路径。
3. 记录 console `error/warning`、失败请求、重复请求和关键任务 ID；401、404、超时都要归类并解释。
4. 至少覆盖：登录、页面切换、会话选择、画布平移/缩放、节点选择、详情、预设、COS、图库、生成按钮状态、刷新恢复和图片放大。
5. 修复后从干净浏览器上下文重复一次，不沿用旧缓存作为通过依据。

本地静态检查：

```bash
PYTHONPATH=.. pytest -q
python -m compileall -q core features generation tasks webui prompts studio cos main.py
python scripts/generate_dashboard_fallback.py
python scripts/check_dashboard_fallback.py
git diff --check
```

## 7. 发布门槛与回滚

- 前端源文件只改 `pages/dashboard/index.html`；`webui/index_fallback.py` 必须由生成脚本更新，不能手工分叉。
- 每次发布前备份远程插件代码目录；不要删除或覆盖 `/root/AstrBot/data/plugin_data/astrbot_plugin_selfie_image`。
- 先通过本地测试和两种视口的真实浏览器回归，再同步代码并重启 `/root/astr.sh restart`。
- 发布后核对页面 HTTP 200、关键 API 状态、浏览器 console、插件加载日志和代码哈希。
- 回滚只替换代码目录，恢复对应备份；运行数据、任务记录和媒体缓存保持不动。

当前状态：

| 项目 | 状态 |
| --- | --- |
| 真实桌面/移动端基线已记录 | done |
| 页面职责和状态边界重构 | done |
| 共享选择器、图库缓存和虚拟列表 | done（本地真实 Chromium mock 烟测） |
| 无限画布移动端可读性和搜索抽屉 | done（本地真实 Chromium mock 烟测） |
| 生成按钮状态和任务恢复 | done（静态回归 + 本地真实 Chromium mock 烟测） |
| 两种视口完整回归并发布 | pending（Token 已验证；远程仍提供旧版 bundle，尚未同步代码或重启） |

最新真实浏览器记录（2026-09-13）：使用干净 Chromium 上下文和两种视口登录远程页面成功；工作台、无限画布和创作画布可打开，桌面/移动端均无横向溢出。远程页面仍出现 1 次 `401 GET /api/config`，以及已清理 `generated_*.png` 导致的 `404 /api/cache-image`；页面源码未包含本轮的 `PICKER_PAGE_SIZE` 和 `REQUEST_TRACE` 标记，因此本轮本地修复尚未部署到远程。
