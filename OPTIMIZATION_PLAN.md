# Selfie Image 代码审计与优化计划

> 目的：围绕空实现、重复实现、逻辑错误和前端反人类操作建立可复核的修复清单。
> 本文只把仓库中有代码证据的问题列为“已确认”；没有运行时复现的内容标为“待验证风险”，不提前改动行为。

## 1. 审计范围与判定规则

### 1.1 入口范围

- 插件与任务生命周期：`main.py`、`tasks/task_manager.py`、`tasks/task_views.py`。
- 生成与存储：`generation/`、`core/providers.py`、`studio/`、`prompts/`。
- 独立 Web：`webui/web.py`，包含 Flask 路由和一份内嵌 `INDEX_HTML`。
- Dashboard Web：`webui/dashboard_api.py`、`pages/dashboard/index.html`。
- 回归基线：`tests/` 中的 Python 测试和页面源码契约测试。

### 1.2 状态标签

- **已确认**：静态代码即可证明，或已有稳定复现路径。
- **待验证**：结构上有风险，必须先用测试或运行时请求确认。
- **正常抽象**：看起来像空实现，但属于接口契约，不作为缺陷修复。
- **已修复**：后续提交完成实现并通过对应验收；没有验收证据不得使用此状态。

### 1.3 约束

1. 先补最小复现和回归测试，再改业务逻辑。
2. 不因为“看起来重复”直接合并独立 Flask 和 AstrBot Dashboard；两者鉴权、启动方式和响应包装不同。
3. 涉及任务、媒体、记录或预设文件的修改，必须说明迁移、备份和回滚路径。
4. 所有异步失败都要有日志或用户可见状态，不能用空 `except` 掩盖业务失败。

## 2. 当前基线

| 项目 | 代码事实 |
| --- | --- |
| 插件入口 | `main.py` 的 `SelfieImagePlugin` |
| 独立 Web | `webui/web.py` 的 Flask 工厂和 `/api/*` 路由 |
| Dashboard Web | `webui/dashboard_api.py` 注册插件页接口；页面为 `pages/dashboard/index.html` |
| 任务状态 | `queued`、`running`、终态（成功、失败、取消、过期及投递失败等）保存在 `generation_tasks.json` |
| 生成记录 | SQLite 记录及媒体来源 sidecar；缓存文件位于 `image_cache/` |
| 页面测试方式 | 现有测试直接读取 `pages/dashboard/index.html`，部分测试同时检查 `webui.web.INDEX_HTML` |

每次修改后执行：

```bash
pytest -q
python -m compileall -q features generation tasks main.py
git diff --check
```

需要浏览器行为的项目，增加 Playwright 或等价浏览器测试；不能只靠字符串断言证明交互正确。

## 3. 空实现与静默失败

### E-01：Provider 抽象方法抛出 `NotImplementedError`（正常抽象）

- **证据**：`core/providers.py:210-211` 的 `BaseImageAdapter.generate()`。
- **判定**：这是适配器契约，不是可直接调用的生产实现，标记为“正常抽象”。
- **计划**：补充抽象标记或文档说明，并增加工厂返回的每个具体适配器都实现 `generate()` 的测试；禁止业务路径实例化基类。
- **验收**：具体 provider 的生成测试通过；基类被直接调用时错误信息明确且可诊断。

### E-02：内置预设导入失败时返回空字典（已确认）

- **证据**：`prompts/preset.py:35-41`、`prompts/preset.py:420-427` 捕获所有异常后返回 `{}`。
- **影响**：导入路径、循环依赖或数据错误会让内置图片/视频预设整体消失，用户只看到空列表。
- **计划**：只捕获可预期的兼容性异常；记录 `logger.warning/error`（包含媒体类型和异常）；向管理页返回可区分的加载状态，不把“加载失败”伪装成“没有预设”。
- **验收**：模拟导入异常时 API 返回明确诊断字段，日志包含 traceback；正常环境的预设内容和用户自定义覆盖规则不变。
- **实现状态**：已修复。`ImagePresetManager`/`VideoPresetManager` 保存 `load_status`，管理 API 返回 `load_status`；异常记录带 traceback，兼容性缺失与真实损坏分开处理。回归测试覆盖内置 seed 异常和日志诊断。

### E-03：画布预设合并异常被静默吞掉（已确认）

- **证据**：`studio/studio_adapter.py:172-177`、`:200-221` 使用 `except Exception: pass`。
- **影响**：画布列表、预设选择器可能部分缺项，且无法判断是“确实没有数据”还是读取失败。
- **计划**：将“单条坏数据”和“存储整体不可读”分开处理；单条跳过并记录名称，整体失败返回错误状态；前端保留旧数据并提示重试。
- **验收**：覆盖文件损坏、单条字段错误、空文件和正常数据四种情况；不能静默返回空列表。
- **实现状态**：已修复。`StudioStore.storage_status()` 区分新文件、整体 JSON 损坏和单条坏数据；Dashboard 在整体失败时保留旧内容并提示重试。存储回归测试覆盖损坏、坏行和空数据。

### E-04：外部页面读取失败只回退、不记录原因（已确认）

- **证据**：`webui/web.py:4551-4559` 读取 `pages/dashboard/index.html` 失败后直接 `pass` 并使用内嵌页面。
- **影响**：部署包缺文件或权限错误会被掩盖，实际运行页面可能已经过期；未被 `OSError` 覆盖的读取异常又可能直接打断启动，错误边界不一致。
- **计划**：记录一次带路径和异常类型的 warning；启动自检报告实际页面来源；保留回退以兼容打包环境。
- **验收**：外部文件不存在、不可读、读取成功时分别可从日志和健康信息判断最终使用的页面来源。
- **实现状态**：已修复。外部页面读取失败记录路径与异常，健康接口返回 `dashboard_page.source/path/error`；fallback 由生成脚本产出并有 hash 一致性检查。

### E-05：前端空 `catch` 造成“无变化”假成功（已确认）

- **证据**：`pages/dashboard/index.html:7122` 画布轮询吞掉异常；另有多处 `catch (_) {}` 用于加载和状态恢复。
- **计划**：区分浏览器存储兼容性异常与业务请求异常；业务请求至少写入当前面板状态、停止或退避轮询，并提供重试入口。
- **验收**：断网、401、500、响应格式错误时，按钮恢复可用，用户看到明确失败原因，控制台和日志不泄露敏感数据。
- **实现状态**：已修复主要业务路径。画布保存/运行/轮询、测试任务轮询、列表加载均有可见错误、退避或重试/取消入口；浏览器存储兼容性异常仍按兼容性边界处理。

## 4. 重复实现与维护源

### D-01：Dashboard 页面源码维护两份（已确认）

- **证据**：历史版本曾在 `webui/web.py` 内嵌完整页面；当前运行时由 `pages/dashboard/index.html` 提供外部唯一编辑源，`webui/index_fallback.py` 是生成产物。
- **影响**：正常部署使用外部文件，但测试和回退可能使用旧副本，修复容易出现漂移。
- **计划**：确定唯一源（建议 `pages/dashboard/index.html`），构建或发布时生成内嵌回退；生成文件必须可追溯，禁止手工维护两份源码。
- **验收**：源码只保留一个编辑源；构建检查能发现外部页面与回退产物不一致；现有 Flask 和 Dashboard 页面测试均通过。
- **实现状态**：已修复。`scripts/generate_dashboard_fallback.py` 与 `scripts/check_dashboard_fallback.py` 固定生成/校验流程，测试同时检查外部页和 fallback。

### D-02：独立 Flask 与 Dashboard API 重复编排业务（已确认）

- **证据**：`webui/web.py:4619` 之后实现配置、任务、记录、资产、缓存、画布和预设路由；`webui/dashboard_api.py:49-152` 再注册同一业务集合。
- **影响**：字段、状态码、脱敏和错误文本可能逐渐漂移；修复一个入口不一定修复另一个入口。
- **计划**：保留两套鉴权/路由适配层，将实际业务下沉到共享 service/contract 函数；先建立同一 fake plugin 的响应契约矩阵，再逐路由迁移。
- **验收**：成功、参数错误、未授权、冲突、能力不存在（200/400/401/409/501）在两入口字段和语义一致；不改变现有鉴权边界。

### D-03：任务 runner 在图片、视频、LLM、画布入口重复（待验证风险）

- **证据**：`main.py` 多处定义同形 `runner`/`runner_with_duration`，例如 `:1272`、`:3247`、`:5240`、`:5734`、`:5802`、`:5875`、`:6526`、`:6644`；均围绕创建任务、检查取消、生成、发送和记录结果编排。
- **风险**：新增状态或错误分支时只修改部分入口，导致取消、重试和投递失败语义不一致。
- **计划**：先画出任务状态转换图，再抽取共享的“生成结果归一化、投递结果标记、异常分类”函数；保留图片/视频特有参数在薄适配器中。
- **验收**：同一状态矩阵覆盖命令、LLM、Web、画布四类入口；重复代码减少且行为快照不变。
- **实现状态**：已完成首版共享编排。`generation/generation_results.py` 提供统一的结果证据判断、协作式取消决策和终态分类；命令、Web、画布 runner 已接入，LLM 入口通过命令 runner 复用。图片/视频特有参数和投递适配仍保留在薄 runner 中；状态快照与四类入口竞态回归已覆盖。

### D-04：兼容路由别名未集中声明（已修复）

- **证据**：`webui/dashboard_api.py:79-88` 为测试任务注册多个取消路径；`:150-152` 同时注册裸路径和 `/page/` 路径；Flask 侧也有对应别名。
- **计划**：集中维护路由契约表，明确每个别名的兼容期限、鉴权和响应包装；没有调用方的别名不得继续复制 handler。
- **验收**：契约表与实际注册路由一致；旧路径有回归测试，新路径不会产生重复副作用。
- **实现状态**：已修复。`webui/contracts.py` 集中声明 Flask/Dashboard 取消别名和 `/page/` 前缀；回归测试校验契约集合，handler 仍只执行一次。

## 5. 已确认的逻辑错误与待验证风险

### L-01：清空/切换测试模式会遗弃后台任务（已修复，P0）

- **证据**：`pages/dashboard/index.html:5758-5782` 的 `switchTestMode()` 调用 `clearTestData(false)`；`5941-5945` 的 `runMediaTest()` 开始新测试前也调用清理；`5994-6005` 的 `clearTestData()` 只清 timer、任务 ID、localStorage 和结果，不调用取消任务 API。
- **影响**：后台任务继续消耗渠道额度并写入记录，但页面失去任务 ID 和取消入口；用户以为“清空”已经停止。
- **修复方向**：把“清空展示”和“取消后台任务”拆成两个显式动作。切换模式或新提交遇到活动任务时，弹窗提供“取消并清空 / 保留后台运行 / 返回”；保留运行时必须仍可在任务历史找到并操作。
- **验收**：提交后立即清空、切换模式、重复提交、关闭再打开页面四条路径均能找回或取消任务；后台协程、任务 JSON 和额度预留状态最终一致。
- **实现状态**：已修复代码路径。清空展示与后台取消分离；切换/重复提交提供取消、保留后台运行或返回；任务历史继续暴露后台任务。浏览器端到端四路径仍建议在真实 AstrBot 宿主补跑。

### L-02：画布保存失败可能继续运行旧配置（已修复，P0）

- **证据**：`pages/dashboard/index.html:7051-7070` 的 `studioSave()` 对业务失败只做局部提示，但网络/解析异常会继续向上抛出；`7084-7098` 的 `studioRun()` 直接 `await studioSave()`，没有统一捕获和状态恢复。
- **影响**：保存失败时用户可能看不到稳定的错误提示，继续生成时使用旧画布数据；运行按钮和状态文字也可能与实际不一致。
- **修复方向**：`studioRun()` 必须把保存、提交、轮询放入统一状态机；保存失败时不提交运行请求，恢复按钮并保留编辑内容。
- **验收**：模拟 400/401/500、网络断开和保存超时时，运行 API 不被调用，错误显示在画布区域，按钮可再次点击。
- **实现状态**：已修复。`studioRun()` 捕获保存与提交异常，保存失败不会调用运行 API，并恢复按钮/保留编辑内容。

### L-03：画布轮询异常后页面停滞（已修复，P1）

- **证据**：`pages/dashboard/index.html:7104-7123` 的定时轮询在异常时空 `catch`，不停止、不退避、不提示。
- **修复方向**：记录连续失败次数；短暂网络错误采用退避重试，鉴权/任务不存在立即停止并提示；提供“重新查询”和“取消任务”按钮。
- **验收**：连续 3 次网络失败、401、任务 404、终态响应分别验证轮询计时器、按钮状态和提示文本。
- **实现状态**：已修复。轮询连续失败采用退避，终止后提供重试/取消；401、404 由 API 错误进入可见状态。现有回归覆盖页面契约，真实浏览器时序仍建议补跑。

### L-04：监控统计口径与分页总数不一致（已修复，P1）

- **证据**：`pages/dashboard/index.html:4341-4360` 使用当前 `rows` 计算成功率、失败率和平均耗时，却同时展示 `RECORD_META.filtered/total` 的筛选全集数量；`webui/web.py:3121-3139` 的内嵌页面存在同类逻辑。
- **影响**：翻页或筛选后，统计卡片的分子、分母和“总计”不是同一范围，用户会误判渠道质量。
- **修复方向**：后端返回筛选全集聚合值，或明确所有指标均为“当前页”；卡片显示统计范围和样本数。
- **验收**：0 条、1 页、多页、筛选后、最后一页分别核对成功率、失败率、平均耗时和总数；外部页面与回退页面一致。
- **实现状态**：已修复。两入口返回筛选全集 `scope_stats`，前端空集合显示“暂无样本”并使用统一分母；fallback 一致性检查通过。

### L-05：任务终态和取消竞态需要完整状态矩阵（部分完成）

- **关注路径**：生成完成但发送失败、取消请求与生成完成同时到达、进程重启恢复、重试原任务、未知投递结果。
- **计划**：以 `tasks/task_manager.py`、`main.py:2469-2517` 及 Web 取消/重试 API 为基准，列出每个状态允许的操作和幂等结果；禁止用“取消”覆盖已有的投递失败或生成成功证据。
- **验收**：每种竞态至少有单元测试；任务状态、生成记录状态、缓存文件和用户提示四者一致。
- **实现状态**：部分完成。已加入 `task_operation_capabilities()` 状态矩阵，取消采用协作式边界且不覆盖生成成功/投递失败证据；取消与晚到成功、投递失败、未知投递、取消前失败记录、部分成功均有回归。进程重启的 `expired` 转换已有 fake runtime 覆盖；真实消息投递、宿主重启恢复与 quota 释放仍待真实宿主运行时矩阵补齐。

### L-06：双 API 的错误包装和字段漂移需要契约测试（待验证风险）

- **关注路径**：`webui/web.py` 的 `success/error` envelope 与 `webui/dashboard_api.py:155-168` 的 Dashboard 包装、501 兼容分支、脱敏字段。
- **计划**：同一请求矩阵对比 HTTP 状态、顶层字段、分页元数据、错误消息和敏感字段；差异必须有兼容理由并写入契约。
- **实现状态**：已完成首版契约矩阵。两入口继续保留各自鉴权/传输适配，但任务、记录、资产、缓存、预设、健康和错误包装已按同一 fake plugin 对照成功/400/401/409/501 语义；共享路由别名、分页元数据、`scope_stats`、脱敏字段和预设加载状态均有回归。Dashboard 的 token-free `auth/check` 与 Flask Token 鉴权是宿主边界差异，已在契约中保留；仍建议真实 AstrBot 版本升级时补跑宿主适配测试。

## 6. 前端交互优化

### U-01：任务控制必须可见且可逆（P0）

- “清空结果”只清理展示；“取消任务”才停止后台任务，两个动作使用不同按钮和确认文案。
- 有活动任务时显示任务 ID、阶段、预计状态和取消入口；不能因为切换 Tab 或模式隐藏唯一控制入口。
- 页面重新打开时从 localStorage 恢复追踪，并提供跳转任务历史的入口。
- **实现状态**：已修复主要页面路径。任务 ID、阶段、取消入口和历史列表均可见；清空展示不会删除后台任务。

### U-02：切换模式和重复提交先处理正在运行的任务（P0）

- 图片/视频模式切换、再次点击生成前检查活动任务。
- 提供“取消并切换”“保留任务并切换”“返回”三种明确选择；默认不做破坏性动作。
- 请求提交期间锁定会导致 payload 改变的控件，完成或失败后统一解锁。
- **实现状态**：已修复主要页面路径。模式切换/重复提交先处理活动任务，保存与提交期间恢复按钮状态。

### U-03：异步失败要在操作区域反馈（P1）

- 保存、上传、提交、轮询、加载列表均显示可读错误和重试动作。
- 错误时恢复按钮、停止无效轮询、保留用户输入；不要只依赖 toast 或控制台。
- 日志记录技术细节，页面显示经过脱敏的用户错误。
- **实现状态**：已修复主要业务路径。API 错误统一脱敏，业务操作区显示失败和重试/取消状态。

### U-04：统计卡片标明范围（P1）

- 明确“筛选全集”与“当前页”的区别，成功/失败/平均耗时使用同一分母。
- 无数据时显示“暂无样本”，不要显示看似精确的 0% 和 0.00s 而不解释。
- **实现状态**：已修复。统计使用筛选全集聚合并在空集合显示“暂无样本”。

### U-05：破坏性操作统一确认语义（P1）

- 记录清空、资产删除、缓存清理、画布删除、批量任务删除分别说明影响范围、是否可恢复和是否影响后台任务。
- 确认按钮使用动作名称，危险操作不与普通“保存/刷新”共用位置或颜色。
- **实现状态**：已修复主要路径。记录、资产、缓存、画布、任务、预设和参考图均使用带影响范围的确认文案；后台任务取消仍是单独动作。

## 7. 分期执行计划

### P0：先修复会丢任务或误操作的路径

1. 为测试页清理、模式切换和重复提交补任务生命周期测试，固定“展示清空”和“后台取消”语义。
2. 修复 `studioRun()` 的保存失败传播、按钮恢复和轮询异常处理。
3. 补任务状态矩阵，确认取消不会覆盖生成成功、投递失败和未知结果。

### P1：消除静默失败并统一用户反馈

1. 修复预设加载和画布列表的静默空结果，增加日志与 API 状态字段。
2. 修复监控统计口径，补筛选、翻页和空数据测试。
3. 为前端所有业务 `catch` 建立统一错误处理和重试策略。
4. 为 Flask/Dashboard 入口建立契约矩阵，先统一字段和错误语义，再迁移重复实现。

### P2：收敛维护源和重复生命周期代码

1. 将页面外部文件设为唯一源，生成或校验 Flask 回退产物。
2. 抽取共享 Web service、响应契约和任务结果归一化函数；保留入口层鉴权差异。
3. 清理无调用方的兼容路由和重复 runner，所有删除先有调用方搜索和回归测试证据。

## 8. 验收清单

- [x] `pytest -q` 通过（当前工作树：`413 passed, 7 subtests passed`），新增测试覆盖已修复的 P0/P1 代码路径。
- [x] `python -m compileall -q features generation tasks main.py webui prompts studio tests` 通过。
- [x] `python scripts/check_dashboard_fallback.py` 与 `git diff --check` 通过，无意外生成文件或敏感数据。
- [x] 清空、切换、关闭页面不会主动遗弃后台任务；任务可取消、可追踪、可重试的代码路径已覆盖。
- [x] 画布保存/提交/轮询失败均可见、可恢复，错误不会静默。
- [x] 预设加载失败可从日志和 API 状态诊断，不再伪装成空列表。
- [x] 页面只有一个维护源，外部页面和 Flask 回退内容可自动校验。
- [x] Flask 与 Dashboard 的首版响应字段、状态码、脱敏和分页契约逐路由对照（宿主边界差异已记录；真实 AstrBot 运行时仍建议补跑）。
- [x] 命令、Web、LLM、画布任务共享结果归一化和取消竞态决策；晚到成功、投递失败、未知投递不会被取消覆盖。
- [x] 已有迁移/回滚基础：SQLite 迁移保留 `generation_records.json.bak`，配置导入预检失败恢复旧配置，JSON 写入采用临时文件替换；升级/排障仍建议先备份整个插件数据目录。

## 9. 明确不在本轮范围

- 不在没有证据时扩大缓存上限、任务保留数量或并发量。
- 不把普通图片/视频请求改成自动携带自拍形象。
- 不直接删除独立 Flask 或 Dashboard 入口，不进行大规模目录重构。
- 不把异常统一改成静默回退；兼容性回退必须有日志、来源标识和可测试的边界。
