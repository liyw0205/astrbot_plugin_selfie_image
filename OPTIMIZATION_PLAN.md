# Selfie Image 持续迭代与 Goal 设计计划

> 本文是后续迭代的工作台，不是问题罗列或已完成工作的重复审计。
> 每个新目标都必须有用户问题、可验证结果、影响范围和回滚办法。
> 旧功能优化是可选方向，新功能开发也是可选方向；优先级由证据决定。

## 1. 使用方式

### 1.1 当前起点

仓库已经完成上一轮稳定性与维护性优化。历史结论不再作为新目标重复排查，具体变更以 `CHANGELOG.md` 和 Git 历史为准。

当前实现边界如下：

| 区域 | 当前事实 | 主要入口 |
| --- | --- | --- |
| 插件生命周期 | AstrBot 加载插件后在 `initialize()` 启动服务，在 `terminate()` 停止服务 | `main.py` |
| 独立管理页 | Flask 默认监听 `127.0.0.1:14514`，由 Web Token 保护 | `webui/web.py` |
| 内嵌管理页 | AstrBot Dashboard 使用插件页 API 和宿主登录态 | `webui/dashboard_api.py`、`pages/dashboard/index.html` |
| 页面维护源 | `pages/dashboard/index.html` 是唯一编辑源，`webui/index_fallback.py` 是生成产物 | `scripts/generate_dashboard_fallback.py` |
| 生成任务 | 图片、视频、画布和命令入口共享任务持久化与状态查询 | `main.py`、`tasks/`、`generation/` |
| 持久数据 | 配置、任务、SQLite 记录、缓存、形象、预设和画布会话分开保存 | 插件数据目录 |
| 自动化基线 | Python 测试、编译检查、fallback 一致性检查和 diff 检查 | `tests/`、`scripts/` |

### 1.2 一次迭代只解决一个结果

- 一个 Goal 只写一个主要结果，例如“移动端在弱网下可以恢复任务”，不要同时承诺重做整个 Dashboard。
- 优化目标必须先有复现路径、指标或用户反馈；没有证据的内容只能进入候选池。
- 新功能必须说明不影响哪些旧行为，以及没有配置、没有形象、没有可用渠道时的降级行为。
- 改动跨越前端、后端和数据时，拆成可独立验收的子 Goal，并保留一个 E2E 目标串联它们。
- 不以“代码已修改”作为完成条件，以自动化结果、浏览器实测和后端实测证据作为完成条件。

### 1.3 状态与优先级

状态按以下顺序流转：

`idea -> discovered -> ready -> in_progress -> verify -> done`

无法继续时使用 `blocked`，完成后发现目标失效或被替代时使用 `closed`。不要把“暂时没有时间”标为 `done`。

| 标记 | 含义 |
| --- | --- |
| `P0` | 阻断生成、数据安全、任务可追踪性或发布；本轮优先处理 |
| `P1` | 明显影响成功率、操作成本或维护成本 |
| `P2` | 体验、性能、可观测性或结构性改进 |
| `FE` | 前端或浏览器行为 |
| `BE` | Python、任务、渠道或 API 行为 |
| `DATA` | 配置、SQLite、JSON、缓存或迁移 |
| `E2E` | 至少跨越浏览器、API 和任务/记录的一条真实链路 |

## 2. Goal 发现方法

### 2.1 信号来源

每个候选目标至少关联一个信号，优先级从高到低参考：

1. 用户反馈、失败任务、无法恢复的操作或实际数据损坏。
2. 日志、任务状态、成功率、延迟、缓存增长和渠道失败统计。
3. 浏览器实测中的布局、交互、弱网、刷新和移动端问题。
4. 代码热点：重复分支、复杂状态机、隐式兼容、难以测试的副作用。
5. 新需求或竞品启发。只有完成可行性和兼容性验证后才进入 `ready`。

### 2.2 发现问题时先回答五个问题

- 谁遇到了问题，触发条件是什么？
- 当前行为与期望行为分别是什么？
- 影响是失败、误导、变慢、难以操作，还是缺少能力？
- 能用什么最小输入稳定复现，如何证明修复有效？
- 如果本轮不做，风险、成本或机会损失是什么？

### 2.3 简单评分

候选目标可以用以下非正式评分排序：

`优先级 = 影响范围 x 发生频率 x 证据可信度 / 实施成本`

每项按 1-5 分记录即可。涉及数据丢失、凭据泄露、后台任务失控时直接提升到 `P0`，不因频率低而降级。

## 3. Goal Card 模板

将下方模板复制到本文件的“当前 Goal 看板”或单独 Issue 中。没有填完验收和回滚字段的目标不能进入 `ready`。

```markdown
## GOAL-ID 标题

- 状态：idea
- 优先级：P1
- 类型：优化 / 新功能
- 范围：FE / BE / DATA / E2E
- 提出日期：YYYY-MM-DD
- 负责人：
- 关联信号：Issue、任务 ID、日志摘要、指标或复现链接

### 用户问题

### 目标结果

用一句可以观察或测量的话描述完成后的行为。

### 成功指标

- 基线：
- 目标：
- 测量方法：

### 范围

- 要做：
- 不做：
- 兼容行为：

### 验收条件

- [ ] 自动化测试：
- [ ] 前端 Termux 浏览器实测：
- [ ] 后端 lmm-server 实测：
- [ ] E2E 或回归：
- [ ] 错误、取消、刷新和重试路径：

### 数据与发布

- 是否改配置、SQLite、JSON、缓存或 API 字段：
- 迁移前备份：
- 回滚动作：
- 发布开关或兼容窗口：

### 证据

- 测试命令与结果：
- 浏览器实测时间、设备、URL 和结果：
- 后端实测时间、远端 commit、任务 ID 和脱敏日志：
```

## 4. 候选方向

下列条目只是发现入口，不代表已经承诺开发。选中后必须先补 Goal Card 和复现/指标。

### 4.1 旧功能优化（可选）

| 候选 ID | 观察信号 | 优先检查 | 可量化结果 |
| --- | --- | --- | --- |
| `OPT-FE-001` | 手机端页面需要频繁缩放、滚动或重复操作 | `pages/dashboard/index.html` 的导航、表单、任务和画布流程 | 目标设备完成关键流程的步骤数、错误率和恢复时间 |
| `OPT-FE-002` | 弱网、401、500 或刷新后状态不清楚 | 列表加载、任务轮询、上传、保存和重试按钮 | 失败后可见提示、可重试，且不重复提交 |
| `OPT-BE-001` | 渠道超时、重试或回退时难以判断实际请求次数 | `core/`、`generation/`、`tasks/` 的超时和重试路径 | 同一目标不重复提交；任务状态和错误类别可追踪 |
| `OPT-BE-002` | 排队时间、并发槽位或额度消耗与用户感知不一致 | `main.py` 任务队列、配额预占和发送状态 | 状态时间线、额度释放和终态在重启/取消后仍一致 |
| `OPT-DATA-001` | 缓存增长、共享媒体或清理预览难以解释 | `generation/`、缓存清理和资产引用 | 清理计划可预览、可回滚；不删除仍被引用的媒体 |
| `OPT-API-001` | 独立 Flask 与内嵌 Dashboard 的字段或错误提示漂移 | `webui/services.py`、`webui/web.py`、`webui/dashboard_api.py` | 同一请求矩阵的字段、状态码和脱敏规则一致 |
| `OPT-OPS-001` | 部署后才发现页面、配置或渠道不可用 | 健康接口、启动日志、配置预检 | 失败在启动/健康检查阶段可定位，不伪装成空数据 |
| `OPT-DOC-001` | 新用户不知道入口、Token、数据目录或排障步骤 | `README.md`、面板帮助和错误文案 | 按文档完成首次配置，且不暴露凭据 |

### 4.2 新功能开发（可选）

| 候选 ID | 用户机会 | 最小可交付版本 | 前置验证 |
| --- | --- | --- | --- |
| `FEAT-CREATE-001` | 复用成功结果的提示词、参数和参考图 | 从一条记录恢复为新任务，保留来源链路 | 记录字段、媒体权限和重试语义 |
| `FEAT-CREATE-002` | 批量创作需要比较变化 | 有限数量的提示词/参数变体，逐项显示任务状态 | 额度上限、取消、部分成功和缓存策略 |
| `FEAT-VIDEO-001` | 视频创作需要更清晰的分镜预览 | 预览分镜并在确认后提交一次任务 | storyboard 解析、时长校验和失败记录 |
| `FEAT-ASSET-001` | 资产较多时需要组合筛选和收藏 | 服务端筛选、标签和最近使用入口 | 分页、权限、媒体缓存和删除保护 |
| `FEAT-OPS-001` | 管理员需要判断渠道是否值得重试 | 脱敏健康摘要、最近失败类别和延迟 | 不显示 API Key，不能因诊断自动改变路由 |
| `FEAT-ADMIN-001` | 多人使用时需要可审计的管理操作 | 管理动作、操作者和结果的审计记录 | 隐私边界、保留期限和导出脱敏 |
| `FEAT-EXT-001` | 预设和创作配置需要跨实例迁移 | 带版本与预检的导入/导出 | 备份、冲突策略、失败恢复和旧版本兼容 |

## 5. 交付流程与验收门

### G0: Goal 定义

- Goal Card 已完成，范围和不做项明确。
- 有最小复现、用户故事或机会证据。
- 明确是否需要 `FE`、`BE`、`DATA` 和 `E2E` 验收。

### G1: 本地自动化

每次代码改动至少执行：

```bash
pytest -q
python -m compileall -q features generation tasks main.py webui prompts studio
python scripts/check_dashboard_fallback.py
git diff --check
```

修改 `pages/dashboard/index.html` 后，先生成再校验 fallback：

```bash
python scripts/generate_dashboard_fallback.py
python scripts/check_dashboard_fallback.py
```

新增或修改共享 API、任务状态、存储格式时，必须补对应回归测试；不能只改页面字符串或只测 happy path。

### G2: 前端 Termux 浏览器实测

前端目标必须在本地 Termux 的真实浏览器打开并操作。源码契约测试可以作为补充，但不能代替浏览器行为验证。

#### 前置条件

- 本地 AstrBot 和插件已启动，或已有本地可用的 Flask 面板实例。
- `web.enable` 已开启，监听地址和端口与测试 URL 一致；默认 URL 为 `http://127.0.0.1:14514/`。
- 使用测试渠道、测试形象和可清理的插件数据，不在浏览器中粘贴生产密钥。

#### 打开页面

```bash
curl -i http://127.0.0.1:14514/api/health
termux-open-url http://127.0.0.1:14514/
```

如果 Termux 没有 `termux-open-url`，在设备浏览器手工打开同一 URL；不要把 `0.0.0.0` 当作浏览器访问地址。Token 只从本地测试配置或启动日志读取，不写入截图、Issue 或 Git。

#### 每个前端 Goal 的最小测试矩阵

| 场景 | 至少验证 |
| --- | --- |
| 设备与布局 | 手机竖屏；必要时横屏；导航、弹窗、长文本、按钮和媒体不重叠 |
| 正常流程 | 登录、填写、提交、加载、完成；只发生一次提交 |
| 任务流程 | 排队、运行、成功、失败、取消、重试、刷新后恢复 |
| 网络异常 | 断网或延迟时有可见错误，按钮恢复，轮询停止或退避 |
| 数据异常 | 400、401、404、409、500 和空列表显示正确状态 |
| 输入与媒体 | 大文本、中文、长文件名、图片上传、重复点击和返回/重新打开 |
| 持久性 | 切换 Tab、刷新页面、关闭再打开后，输入和任务语义符合 Goal |

实测记录至少包含：设备/浏览器、URL、测试时间、复现步骤、预期与实际结果。截图和录屏放在仓库外的临时目录，例如 `/tmp/selfie-goal/<GOAL-ID>/frontend/`，不提交到 Git。

### G3: 后端 lmm-server 实测

后端开发需要通过以下 SSH helper 在远端运行真实环境验证。计划文档只引用 helper 路径，不保存其内部凭据：

```bash
LMM_SERVER=/data/user/0/com.termux/files/home/.ssh/lmm-server.sh
```

#### 先确认远端状态

将 `<remote-plugin-dir>`、`<astrbot-log-command>` 和端口替换为实际部署值。不要未经确认执行会覆盖配置或删除数据的命令。

```bash
"$LMM_SERVER" 'cd <remote-plugin-dir> && pwd && git rev-parse --short HEAD && python3 --version'
"$LMM_SERVER" 'cd <remote-plugin-dir> && git status --short'
"$LMM_SERVER" 'curl -fsS http://127.0.0.1:14514/api/health'
```

#### 最小后端验证

```bash
"$LMM_SERVER" 'cd <remote-plugin-dir> && pytest -q'
"$LMM_SERVER" 'cd <remote-plugin-dir> && python3 -m compileall -q features generation tasks main.py webui prompts studio'
"$LMM_SERVER" 'cd <remote-plugin-dir> && python3 scripts/check_dashboard_fallback.py'
```

涉及真实渠道时，再执行一次有边界的 smoke test：请求数量固定为 1，使用测试模型/测试渠道，设置明确超时，记录任务 ID，并确认缓存、记录和通知状态。除非 Goal 明确要求，不进行批量生成、无限重试、配置导入或缓存清理。

后端实测必须观察完整链路：请求校验 -> 任务创建 -> 排队/运行 -> provider 调用 -> 记录与缓存 -> 发送状态 -> 终态查询。失败时同时检查错误类别、任务状态、额度释放、是否重复提交和日志脱敏。日志命令由远端部署决定，不把包含 Token、API Key、Cookie 或 SSH 信息的原始输出带回仓库。

#### 远端证据

在 Goal Card 中记录：远端 commit、命令、执行时间、HTTP 状态、任务 ID、关键脱敏日志和数据目录备份位置。远端工作树若不是目标 commit，先标记 `blocked`，不要用不确定版本的结果关闭 Goal。

### G4: 跨层 E2E

需要 `E2E` 的 Goal 至少跑一次以下链路：

1. Termux 浏览器发起请求。
2. 后端创建并持久化任务。
3. 任务成功或失败后，浏览器轮询/刷新得到同一终态。
4. 记录、媒体缓存、发送状态和用户提示互相一致。
5. 取消、重复点击、页面刷新或进程重启后仍符合 Goal 的约束。

### G5: 发布关闭

- G1、G2、G3 和需要的 G4 均有结果，不接受“未测试”作为 `done`。
- `git diff --check` 通过，fallback 与页面源一致。
- 配置、存储、API 字段和兼容路由的变更已写入 `CHANGELOG.md` 或发布说明。
- 变更影响、监控信号、回滚动作和遗留风险已写回 Goal Card。

## 6. 数据、迁移与回滚

涉及以下内容时，开发前先备份整个插件数据目录，并在 Goal Card 写明备份位置：

- `selfie_image_config.json` 或 AstrBot 启动配置。
- `generation_tasks.json`、`generation_records.sqlite3` 及旧版 JSON 备份。
- `image_cache/`、`media_sources/`、`image-persona/`、预设和 `studio_sessions.json`。

迁移要求：

- 优先向后兼容读取，写入使用临时文件替换或事务。
- 新字段提供默认值，旧数据缺字段不能导致整个列表为空。
- 迁移失败必须保留旧数据并返回可诊断错误，不能静默覆盖。
- 删除、清理和批量操作先提供预览或确认；后台活动任务不能被展示层清理误删。

回滚要求：

- 代码回滚与数据回滚分别设计；不要用代码回滚代替数据库恢复。
- 能力开关关闭后，旧 API、旧页面和旧数据仍能安全读取。
- 真实渠道变更后保留一段观察窗口，确认失败率、延迟、额度和缓存增长正常再关闭 Goal。

## 7. 当前 Goal 看板

本表只放正在设计或执行的目标。已完成的历史优化不在这里重复列出。

| ID | 类型 | 范围 | 优先级 | 状态 | 下一步 | 验收证据 |
| --- | --- | --- | --- | --- | --- | --- |
| `OPT-OPS-001` | 优化 | BE / E2E | P1 | done | 观察启动日志与 health 状态，确认无配置漂移 | 本地/远端测试、health 200、源码哈希一致 |

### OPT-OPS-001 启动与健康配置预检

- 状态：done
- 优先级：P1
- 类型：优化
- 范围：BE / E2E
- 提出日期：2026-09-11
- 负责人：Codex
- 关联信号：当前 `/api/health` 固定返回 `status: ok`；配置保存虽执行渠道预检，但启动和健康接口不暴露无效/未配置渠道，部署故障只能在首次生成时发现。

#### 用户问题

管理员打开健康接口时无法区分“服务正常”“尚未配置生成渠道”和“渠道配置缺少地址/密钥”。空数据看起来像成功，排障被推迟到真实生成请求。

#### 目标结果

启动日志和独立 Flask、内嵌 Dashboard 的健康响应都提供不含凭据的 `config_preflight` 摘要；无可用生成渠道时健康状态为 `degraded`，并给出字段级诊断。

#### 成功指标

- 基线：健康响应始终为 `status: ok`，没有配置预检摘要。
- 目标：预检状态可区分 `ok`、`unconfigured`、`unavailable`、`invalid`、`error`；诊断不包含 API Key、Token 或代理密码。
- 测量方法：预检单测 + Flask test client + Dashboard handler 契约测试；完整本地验收命令通过。

#### 范围

- 要做：复用现有 `preflight_config_channels`；增加健康摘要、启动 warning/info 日志、两入口字段一致性和回归测试。
- 不做：不发起网络探测、不自动修改有效渠道、不改变生成路由和鉴权状态码。
- 兼容行为：健康接口仍返回 HTTP 200；没有新方法的旧测试桩保持 `status: ok` 且 `config_preflight` 为空。

#### 验收条件

- [x] 自动化测试：配置预检状态、脱敏和健康摘要测试通过。
- [x] 前端 Termux 浏览器实测：本目标无页面交互改动；健康接口由后端真实运行实例验证，未改变页面布局/交互。
- [x] 后端 lmm-server 实测：`/data/user/0/com.termux/files/home/.ssh/lmm-server.sh`；部署目录已备份并同步，远端 pytest/compileall/fallback 通过。
- [x] E2E 或回归：Flask 与 Dashboard 共用 `build_health_payload`，均有 handler/契约回归。
- [x] 错误、取消、刷新和重试路径：本目标不改变任务操作；配置异常只影响健康诊断。

#### 数据与发布

- 是否改配置、SQLite、JSON、缓存或 API 字段：不改持久化格式；健康响应新增字段，向后兼容。
- 迁移前备份：无需迁移；不写配置。
- 回滚动作：回滚代码即可，旧配置和任务数据无需恢复。
- 发布开关或兼容窗口：无需开关；观察启动日志与健康状态，确认配置完成后状态回到 `ok`。

#### 证据

- 测试命令与结果：本地 `pytest -q` -> `430 passed, 7 subtests passed`；本地 compileall、`scripts/check_dashboard_fallback.py`、`git diff --check` 均通过。远端 `/root/myenv/bin/pytest -q` -> `430 passed, 7 subtests passed`，compileall/fallback 通过。
- 浏览器实测时间、设备、URL 和结果：2026-09-11，未改前端交互；远端真实服务 `http://127.0.0.1:14514/api/health` 认证后 HTTP 200，返回 `status=ok`、`config_preflight.status=ok`、`ready=true`、`channel_count=10`、`ready_count=5`、`invalid_count=0`。
- 后端实测时间、远端 commit、任务 ID 和脱敏日志：2026-09-11；更新前部署基线 `cc0b34750`，备份 `/root/AstrBot/data/plugins_backup/astrbot_plugin_selfie_image.pre-opt-ops-001-20260911-234911`；重启后日志记录“渠道配置预检通过: 5/10 个生成渠道可用”。本地与远端关键源码 SHA-256 一致；无生成任务/真实渠道请求。

建议每次只把一个目标推进到 `in_progress`；其他候选保留在 `idea` 或 `discovered`，避免同时改变任务、存储和页面却无法定位回归来源。

## 8. 已完成基线（仅供上下文）

上一轮已完成的方向包括：任务取消与终态竞态、生成/发送状态区分、预设和画布加载失败诊断、前端异步错误反馈、统计口径、Flask/Dashboard 契约、页面 fallback 单一源、记录/资产/缓存保护以及相关回归测试。

这些内容默认视为基线。除非出现新的运行时证据，不要重新创建相同的审计条目；发现回归时新建带复现证据的 `REG-*` Goal，并引用对应提交或测试。

## 9. 明确不做

- 没有指标或复现时，不扩大并发、缓存上限、任务保留数量或重试次数。
- 不把普通生图、普通视频或普通 LLM 工具请求静默改成携带自拍形象。
- 不在没有调用方搜索和回归测试的情况下删除 Flask、Dashboard、兼容路由或旧数据字段。
- 不把真实 API Key、Web Token、SSH 信息、浏览器截图、远端日志原文提交到仓库。
- 不在远端实测中执行未获 Goal 明确授权的批量生成、破坏性迁移或清理操作。

## 10. 完成检查表

- [x] Goal Card 的问题、目标、范围、不做项和指标已填写。
- [x] 本地自动化测试通过，新增行为有回归测试。
- [x] 前端验收：本 Goal 无页面交互改动，健康 API 已由真实远端服务验证。
- [x] 后端 Goal 已调用 `/data/user/0/com.termux/files/home/.ssh/lmm-server.sh` 在目标远端实测。
- [x] 需要时已完成 API 入口 -> 健康摘要 E2E；本 Goal 不涉及任务/记录/缓存链路。
- [x] 数据备份、迁移、兼容和回滚步骤已记录并验证；远端部署前已备份代码目录。
- [x] 证据已脱敏、可复现，且没有敏感文件进入 Git。
- [x] 发布说明、监控窗口和遗留风险已记录，状态已改为 `done`。
