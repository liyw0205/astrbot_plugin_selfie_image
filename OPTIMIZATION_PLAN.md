# Selfie Image 开发与优化计划

> 本文件只记录当前仓库能够证明的维护工作。事实以运行中的代码、自动化测试、`CHANGELOG.md` 和 git 历史为准；没有复现、日志或性能数据的内容只能写成候选，不能写成已确认缺陷。

## 1. 当前基线

| 项目 | 当前事实 |
| --- | --- |
| 插件版本 | `metadata.yaml`、`core/constants.py` 均为 `1.6.14` |
| AstrBot 兼容范围 | `>=4.13.0,<5` |
| 插件入口 | `main.py` 的 `SelfieImagePlugin` |
| Web 入口 | 独立 Flask：`webui/web.py`；内嵌 Dashboard：`webui/dashboard_api.py` 与 `pages/dashboard/index.html` |
| 生成记录 | SQLite 索引 `generation_records.sqlite3`；媒体来源放在 `media_sources/` sidecar；旧 JSON 仅用于迁移兼容 |
| 任务记录 | `generation_tasks.json`，任务启动时不会恢复 `queued/running`，会标记为 `expired` |
| 媒体缓存 | `image_cache/`，同时包含图片和 `video/` 子目录；默认上限 100 个文件 / 200 MB |
| 当前测试 | `pytest -q`：`403 passed, 7 subtests passed` |
| 当前失败 | 无；版本文案契约已恢复为 README 的“当前稳定版：`1.6.14`” |
| 静态检查 | `python -m compileall -q features generation tasks main.py` 通过；`git diff --check` 通过 |

测试数字是当前工作树的实测结果，不得在修复 README 或测试契约前写成“全量通过”。

## 2. 已实现边界

以下能力已经在代码和测试中存在，不再作为新功能重复排期：

- 图片、视频、文生图、图生图、自拍、他拍、合影、COS、画布和 LLM 工具入口。
- 多渠道/多模型优先级、API 多密钥轮换、失败分类、有限 fallback、全局超时和批量并发槽位。
- 任务持久化、排队/运行/终态、取消、重试、过滤分页、任务详情、记录 ID 关联和失败兜底记录。
- 生成成功、投递成功、明确投递失败、投递结果未知的独立状态；投递失败时保留缓存并支持重发。
- SQLite 生成记录、媒体来源 sidecar、收藏/置顶/标签/备注、资产筛选、导入导出、记录复用和父记录重试链路。
- 缓存按文件数与字节数统计；清理预览使用 `plan_token`；收藏/置顶媒体受保护；普通记录引用不再永久阻止回收。
- Dashboard 记录/资产分页、页码和总数、元数据页缓存、相邻页预取、媒体请求去重、媒体加载优先级、旧请求序列校验和强制刷新请求。
- 普通 `generate_image` 不会因机器人名称或普通上下文自动进入自拍流程，也不会静默携带主形象；明确自拍工具才携带主形象。
- 内置图像转换预设：`变真人`、`变动漫`、`变猫娘`、`变Q版`、`变像素`、`真人化`、`变COS`、`漫画封面`、`证件照`、`果冻化`。

## 3. 更新日志与 git 历史核对

`CHANGELOG.md` 当前最高版本为 `1.6.14`，其中已经记录了密钥兼容、任务分页、失败记录、视频、SQLite 资产库和图像转换预设等历史能力。

当前 HEAD 是 `b574bb4`。`1.6.14` 日志之后又有以下已落地提交，不能再写成“待开发功能”：

| 提交 | 已落地内容 | 主要文件 |
| --- | --- | --- |
| `4cfb4d3` | 自拍/COS 与 Dashboard 优化及回归覆盖 | `cos/cos_looks.py`、`main.py`、`pages/dashboard/index.html` |
| `7010a5e` | 普通缓存媒体回收、分页稳定性 | `generation/generation_store.py`、Dashboard、存储测试 |
| `8810586` | 记录/资产分页元数据缓存 | Dashboard、前端缓存测试 |
| `fd58138` | 缓存命中时即时渲染分页 | Dashboard、前端缓存测试 |
| `f150513` | 缩小资产媒体预加载窗口 | Dashboard |
| `a7dfe5d` | 强制刷新请求绕过旧响应 | Dashboard、前端缓存测试 |
| `042226e` | 区分 QQ 发送超时与明确投递失败 | `main.py`、任务/结果/媒体模块 |
| `b574bb4` | LLM 角色图片不再误进入自拍流程 | `main.py`、自拍提示词模块 |

这些提交现已在 `CHANGELOG.md` 的 `Unreleased` 条目中汇总。版本继续保持 `1.6.14`，正式发布时再升版本；README、metadata、常量和 CHANGELOG 的稳定版本事实保持一致。

## 4. 当前待处理项

### P0：修复版本文案与测试契约不一致

- **证据**：`core/constants.py` 和 `metadata.yaml` 都是 `1.6.14`；测试精确查找“当前稳定版：1.6.14”（版本号两侧带 Markdown 反引号）；README 当前使用另一种文案。
- **建议**：若没有产品意图改变文案，恢复 README 的稳定版表述；若要保留“元数据版本”，则同步修改测试，使测试校验同一公开契约，而不是同时保留两个表述。
- **范围**：`README.md`、`tests/test_core.py`；不改生成逻辑，不改版本号。
- **验收**：版本号只从 `metadata.yaml`/`core/constants.py` 取值；`pytest -q` 不再有该失败；README 与 CHANGELOG 的最高版本表述一致。
- **状态**：已完成；README、metadata、常量和 CHANGELOG 最高稳定版本均为 `1.6.14`。

### P1：同步 HEAD 之后的更新日志

- **证据**：git 历史已有 `7010a5e`、`8810586`、`fd58138`、`f150513`、`a7dfe5d`、`042226e`、`b574bb4`，但 `CHANGELOG.md` 最高仍是 `1.6.14`。
- **目标**：补充 `Unreleased` 或新的版本条目，准确描述缓存回收、分页缓存/刷新、媒体预加载、投递状态和自拍路由修复；不把尚未验证的性能数字写入日志。
- **范围**：`CHANGELOG.md`，必要时同步 README 的功能概览；不在此项中改变代码版本。
- **验收**：每个 HEAD 功能都有提交或文件依据；已实现功能不出现在“待开发”清单；版本策略由维护者明确记录。
- **状态**：已完成；采用 `Unreleased` 条目，版本号继续保持 `1.6.14`，待正式发布时再升版本。

## 5. 有代码依据的优化候选

候选必须先补最小证据，再决定是否开发。候选不是已确认缺陷。

### O-01：让缓存预览与确认执行同一份清理计划

- **代码事实**：`generation/generation_store.py` 的 `_cache_cleanup_plan()` 生成计划和 token；`cleanup_image_cache_from_web()` 确认时先重新生成计划校验 token，随后 `_cleanup_image_cache_if_needed()` 又重新计算一次。
- **风险**：预览与实际删除之间若有文件新增、删除、收藏/置顶变化，执行计划可能与用户看到的计划不同；现有测试覆盖 token 转发和预览不删除，但没有覆盖该竞态。
- **目标**：确认时执行已签名/已校验的同一组相对路径，或在计划发生变化时明确拒绝并要求重新预览；继续保护收藏/置顶媒体并保留路径越界防护。
- **范围**：`generation/generation_store.py`、`webui/web.py`、`webui/dashboard_api.py`、`tests/test_core.py`/`tests/test_storage.py`。
- **验收**：覆盖无候选、候选被外部删除、候选新增、保护状态变化、图片与视频路径；执行结果只能来自确认的计划；不删除记录本身。
- **优先级**：P1；先写竞态测试再改实现。
- **状态**：已完成；确认入口重新校验 token 后执行同一份相对路径计划，计划项缺失、大小变化或保护状态/候选集合变化时拒绝执行；覆盖无候选、外部删除/新增、保护状态、图片/视频和记录保留。

### O-02：确认任务历史的保留策略

- **代码事实**：`tasks/task_manager.py::_prune_web_tasks_locked()` 在任务超过 50 条时只保留活动任务和较新的终态任务；生成记录上限 `RECORD_KEEP_LIMIT` 为 1000，Dashboard 又提供任务过滤分页。
- **评估结果**：保留 50 条终态任务是最优方案。任务快照服务于运行监控和重试入口，完整历史已经由 SQLite 生成记录承载；扩大任务 JSON 会增加启动加载、脱敏和 Dashboard 分页成本，却不会增加媒体或记录可追溯性。
- **目标**：固定保留数量、终态任务删除/导出语义、记录删除后任务详情的行为，并在 Dashboard 文案和测试中固定下来。
- **范围**：`tasks/task_manager.py`、`tasks/task_views.py`、`webui/web.py`、`webui/dashboard_api.py`、任务测试。
- **验收**：创建超过 50 条终态任务后验证列表、分页、导出、删除、重试和关联记录；若保留 50 条是有意设计，则补文档和回归测试，不做无依据扩容。
- **优先级**：P1；先确认产品保留策略。
- **状态**：已完成；`WEB_TASK_KEEP_LIMIT = 50` 固化为公开契约，活动任务永不因上限删除，终态任务按 `updated_ts` 保留最新 50 条；任务删除/回收不删除生成记录或缓存。README、Dashboard 和回归测试已同步。

### O-03：评估资产查询是否需要使用 SQLite 索引

- **代码事实**：`generation/record_database.py` 建有 `seq` 和资产优先级索引，但 `generation/generation_store.py::query_asset_records()` 当前先取最多 1000 条记录，再在 Python 中过滤、排序和分页。
- **评估结果**：保留 Python 查询路径。真实持久化记录上限为 1000，资产查询显式将扫描窗口限制为同一上限；脱敏 Python 扫描基准在 100/1000/10000 条规模的中位耗时约为 `0.05/0.55/5.57 ms`。在当前产品规模下 SQLite 查询下沉不会带来可证明的收益，反而会增加 JSON payload 查询和兼容迁移风险。
- **目标**：把 1000 条扫描上限写成显式常量，保留结果一致的 Python 筛选/排序/分页，并增加基准和上限回归，达到规模增长时可重新评估的证据闭环。
- **范围**：`generation/record_database.py`、`generation/generation_store.py`、资产 API 与存储测试。
- **验收**：使用 100、1000、10000 条脱敏记录比较筛选、排序、分页耗时和结果一致性；覆盖 SQLite 不可用时的兼容路径；迁移前后导入导出和媒体回收结果一致。
- **优先级**：P2；必须先有基准数据。
- **状态**：已完成评估，暂不下沉 SQLite；`ASSET_QUERY_SCAN_LIMIT = RECORD_KEEP_LIMIT = 1000` 已显式固化，基准结果记录在本条，继续保留现有兼容路径。

### O-04：双 Web API 的契约矩阵回归

- **代码事实**：独立 Flask 和内嵌 Dashboard 分别实现健康、配置、任务、记录、资产、缓存、画布和预设 API；两者大量逻辑平行，部分接口用 `getattr` 为旧对象返回 501。
- **评估结果**：不合并两套服务。两者运行时鉴权模型不同（独立 Flask 使用 Token，内嵌 Dashboard 依赖 AstrBot 登录态），但业务字段和错误语义应一致；最优改法是共享契约断言和最小适配层，而不是重写路由。
- **目标**：同一 fake plugin、同一请求矩阵下，两套入口的字段、状态码、鉴权错误、脱敏结果和投递状态语义一致；兼容旧对象的 501 只在方法确实不存在时出现。
- **范围**：`webui/web.py`、`webui/dashboard_api.py`、`tests/test_core.py`。
- **验收**：覆盖 200/400/401/409/501、成功响应、敏感字段脱敏、分页元数据、缓存确认 token 和记录重试；不在没有重复逻辑痛点证据前合并两套服务。
- **优先级**：P2。
- **状态**：已完成评估并固化：保留双入口；统一健康响应字段（移除仅 Dashboard 的 `source` 字段），任务/资产/缓存路由继续复用相同 plugin 方法、状态码和脱敏规则。新增契约回归覆盖字段和 401/409/501 矩阵。

## 6. 不预登记的新功能

目前没有代码或更新日志支持的新增功能排期。没有明确场景和最小闭环前，不预先加入：

- 收藏夹、虚拟滚动、更多渠道或新的远程测试系统；
- 自动重复投递、扩大缓存上限、扩大任务保留数量；
- 把普通 `generate_image` 改成自动携带主形象；
- 删除生成记录、迁移 SQLite 或清空媒体的破坏性操作。

任何新功能必须说明现有入口为何不足、数据迁移与回滚方式、指令/LLM/Web 是否都要支持，以及自动化和运行时验收标准。

## 7. Goal 登记模板

```markdown
### G-YYYYMMDD-NN 标题

- 类型：文档修复 / 旧功能优化 / 新功能开发
- 优先级：P0 / P1 / P2 / P3
- 来源：提交、测试失败、用户报告、日志或性能采样
- 当前行为：入口、输入、实际响应和代码位置
- 目标行为：可重复验证的一句话结果
- 非目标：本次明确不改变的行为
- 代码范围：文件、类、函数、API 和数据格式
- 风险：兼容性、数据迁移、上游渠道、平台投递、权限
- 自动化测试：测试文件与命令
- 运行时验收：必要时记录浏览器请求、任务 ID、记录 ID 和日志时间
- 部署与回滚：备份、hash、重载方式和恢复路径
- 状态：待证据 / 设计中 / 开发中 / 本地通过 / 运行时通过 / 已完成 / 阻塞
```

## 8. 完成标准

每个 Goal 只有同时满足以下条件才能标记“已完成”：

1. 当前行为和目标行为都能由代码、测试或运行时证据复核。
2. 自动化测试覆盖成功、失败、取消、未知或竞态分支中与本项相关的部分。
3. 文档、版本号、公开 API 和持久化格式没有互相矛盾。
4. 涉及媒体、记录、SQLite 或任务数据时，先说明备份、迁移和回滚方式。
5. 性能优化有前后基准；前端优化至少记录请求数、响应时间和旧请求覆盖情况。

## 9. 提交前检查

```bash
pytest -q
python -m compileall -q features generation tasks main.py
git diff --check
git status --short
```

本文件不保存 Token、API Key、浏览器 profile、截图原图、远程数据副本或未脱敏日志。
