# 目标 05：AstrBot 运行时冒烟契约

## 目标
为不依赖真实 AstrBot 服务的运行时边界建立契约测试，验证插件初始化、配置读取、Web 生命周期和关键命令注册所需的最小接口。

## 范围
- 构造最小 AstrBot 兼容桩并测试初始化、终止和配置加载。
- 验证 Web 启动配置不被独立配置文件覆盖。
- 将运行环境前提和人工烟测步骤写入开发文档。

## 非目标
- 不启动真实生产 Bot。
- 不调用真实 Provider 或使用真实密钥。

## 验收
- 最小运行时桩可完成初始化与终止。
- 缺失可选 AstrBot 能力时返回可诊断错误而非泄露配置。
