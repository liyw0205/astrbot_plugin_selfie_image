# 更新日志

## 1.4.8

- 收紧 `/看看腿` 腿部穿搭提示：仅允许光腿神器、白丝或黑丝，明确覆盖到膝部并排除中筒袜、短袜等普通袜型。

## 1.4.7

- 修复 `/查看提示词` 将当前用户误判为机器人，导致普通引用图片错误命中最近自拍生图记录的问题。

## 1.4.6

- 修复上游生图接口返回图片 URL 后，CDN/代理下载卡满 180 秒被误报为模型超时的问题：生成请求仍使用原超时，结果图片下载独立限制为 30 秒。
- 下载失败记录 URL 主机、状态或超时阶段，隐藏签名查询参数，便于定位代理/CDN 连通性。

## 1.4.5

- 修复 `/查看提示词` 引用 QQ 机器人图片时错误使用远程转码 URL 的问题：依据 AstrBot `Reply.sender_id` 只在确实引用机器人图片时优先校验插件原始缓存，并通过 OneBot `get_image` 解析 QQ 文件标识。
- 兼容 AstrBot 事件中仅保留原始 OneBot 图片字典的情况，避免丢失图片候选来源。

## 1.4.4

- 修复 `/查看提示词` 在 QQ 图片同时存在 `file`、`path` 和远程 `url` 时漏读本地原图，导致 MD5 被远程转码图片覆盖的问题。
- 支持解析 `file:///` 图片路径，优先使用本地原始字节匹配生图记录。

## 1.4.3

- 新增 `/查看提示词`：引用插件生成的图片可按 MD5 找回实际生图提示词；非插件图片由当前聊天 LLM 反推，历史记录的 MD5 字段缺失时显示为空。
- 调整 `/看看腿` 为成年人物的日常下装穿搭记录，降低局部身体描述导致的安全误判。
- 随机使用第一人称手机记录或第三人称朋友协助拍摄视角，默认采用腰部至膝部的自然坐姿、侧向躺靠或休息构图。
- 整理椅上、窗边、桌前、席地、沙发靠坐和床上休息等多种日常构图；用户补充的高风险姿势词会转换为中性表达。
- 同步更新 Persona、英文内置提示词和 Studio 模板文案，移除旧的高风险局部描述。

## 1.4.2

- 新增 `/辅助形象设置` 与 `/辅助形象清除`，辅助形象最多保存 3 张。
- 普通自拍/换装可使用辅助形象；合影仅使用主形象图，避免占用参考图额度。

## 1.4.1

- 混杂心跳大正文不再走 HTML 相对路径正则，避免 base64 回溯卡住 AstrBot。

## 1.4.0

- 等待心跳和最终 JSON 混在一起时，用正则提取 `data:image` / 裸 base64 / 图片链接，不再把整段当非 JSON。

## 1.3.97

- Web 试画、指令和 LLM 工具兼容中继直接返回裸 PNG 或裸 base64，不再只认 JSON。

## 1.3.96

- 新增生图渠道类型 `openai_chat`：给只提供 `chat/completions` 的 GPT 生图中继手动选择，请求体仍是 `{model, prompt, n, size}`。`gpt-image-2` 自动仍是 `openai`。渠道地址会截掉 `/v1` 后面的路径后再拼接口。
- 渠道编辑里已启用模型改为名称单独一行，避免 PC 半宽弹窗把模型名挤没，只剩类型和代理。

## 1.3.95

- `/看看COS` 套装池改为 27 套：姬小满按变装 5 秒后的黑金橙短装入池；离恨烟锁定公孙离，不是大乔；西施四刀分开（同人短旗袍、青绿渐变旗袍、诗语江南、露腰短旗袍）；新增影黑白红短装、露莎白金短装、秧秧蓝白碎花泳装、卡提希娅黑裙飞鸟、青绿双辫广袖长裙。露莎白金短装与青绿双辫不写作品来源。
- 他拍改为别人视角的单人成品照：拍摄者不入镜，不要第二个人，不要画出举着手机拍主角的过程。

## 1.3.94

- `/看看COS` 套装池改为 18 套：加入貂蝉三国杀、铂金发白蕾丝长裙、西施青绿渐变旗袍、黄结白围裙女仆、银紫深V广袖、露背蓝纱古装；女仆与古风/白裙不写作品来源；不加瑶大耳狗；露莎放弃入池。
- 类别 COS（汉服、白裙等）只写可见服装结构，来源锁定改为可选，不写抖音/作者/标题。
- 移除仓库内开发文档与方案稿。
- 露背蓝纱古装改为四分之三侧身构图，并限制只有两只手，减少正面/背面叠画导致的第三只手。
- `/看看COS` 默认随机自拍或他拍，额外提示词可指定机位；他拍时覆盖套装里的对镜描述。

## 1.3.93

- 修复批量生图在并发上限小于批量数量时可能永久卡在排队的问题：每张图片独立等待并发槽位，支持并发 3 生成 10 张并按队列继续。
- 排队提示改为仅在当前并发槽位全部占用时出现；空闲时即使一次请求超过并发上限，也不再提示“上一轮还在画”。
- 插件重启后不恢复旧的排队/运行任务，并将旧任务持久化为已过期，避免重启前任务再次发送。
- `/生图取消` 改为立即取消后台任务，不再等待当前生成步骤结束后额外执行或发送一次。

## 1.3.92

- 修复单条批量生图默认上限为 2，导致 `/看看腿 10` 在创建任务前被截断的问题。
- 将默认单条批量上限调整为 10；全局并发数量仍由 `max_concurrent_tasks` 独立控制。
- 同步独立 Web 与内嵌 Dashboard 的批量上限默认值，并增加配置回归验证。

## 1.3.91（稳定版）

- 修正《无职转生》洛琪希奶油睡衣 COS 的来源说明。
- 将洛琪希姿势明确为地毯上双膝并拢的跪坐，双腿收拢压在身下，排除盘腿、分腿和站立姿势。
- 增加洛琪希 COS 姿势与来源的回归测试。

## 1.3.90（稳定版）

- 调整 `/看看COS` 的参考图收集：保留当前消息及引用图片，但不再自动继承历史上下文图片。
- 将 COS 随机套装池整理为 13 套结构明确的真人换装提示词，减少复杂且不稳定的服装描述。
- 增加 COS 历史图片隔离、当前消息图片保留和套装池完整性回归测试。

## 1.3.89

- 增加管理面板统一的保存配置与刷新数据入口。
- 完善页面进入和手动刷新时的数据加载。
- 修复渠道列表启用/禁用开关无法直接持久化、刷新后状态丢失的问题。

## 1.3.88

- 增加后台任务短期幂等去重与强制重生成入口，减少重复扣费。
- 增加渠道健康度记录、网络/服务端失败短暂冷却和 Dashboard 手动清除入口。
- 增加配置 schema 版本、脱敏导出、导入预览与失败回滚。
- 增加图片发送失败保留与 `/生图重发` 命令。
- 增加缓存清理预览和任务持久化可靠性改进。
- 按 Bento Studio 方案重做管理面板，并让 Flask Web 与 AstrBot 内置 Web 共用 `pages/dashboard/index.html`。

## 1.3.86

- 优化生图任务并发调度：同时并发上限按所有任务合计计算，不再锁在单条指令或单个批次内。
- 修复批量生图时的排队判断：当前还有可用并发槽位时，新指令可以立即接入。
- 修复排队提示回复时机：指令回复阶段即可提示排队状态，不再晚于生成进度提示。
- 生图同时并发上限默认从 3 调整为 10，最大支持 10 张同时请求上游。
- 更新独立 Web 面板和内嵌 Dashboard 的并发配置说明。
- 10 并发真实渠道测试通过：`gpt-image-2` 渠道 10/10 请求成功。

## 1.3.85

- 修复看看腿构图裁切限制，恢复蕾丝与吊带袜相关提示约束。

## 1.3.83

- 隔离每个模型尝试的工作线程，避免单个模型请求阻塞其他模型流程。
