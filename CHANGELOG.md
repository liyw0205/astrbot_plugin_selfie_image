# 更新日志

## 1.3.93

- 修复批量生图在并发上限小于批量数量时可能永久卡在排队的问题：每张图片独立等待并发槽位，支持并发 3 生成 10 张并按队列继续。
- 插件重启后不恢复旧的排队/运行任务，并将旧任务持久化为已过期，避免重启前任务再次发送。
- `/生图取消` 改为立即取消后台任务，不再等待当前生成步骤结束后额外执行或发送一次。

## 1.3.92

- 修复单条批量生图默认上限为 2，导致 `/看看腿 10` 在创建任务前被截断的问题。
- 将默认单条批量上限调整为 10；全局并发数量仍由 `max_concurrent_tasks` 独立控制。
- 同步独立 Web 与内嵌 Dashboard 的批量上限默认值，并增加配置回归验证。

## 1.3.91（稳定版）

- 修正《无职转生》洛琪希奶油睡衣 COS 的来源说明。
- 将洛琪希姿势明确为地毯上双膝并拢的跪坐，双腿收拢压在身下，排除盘腿、分腿和站立姿势。
- 增加洛琪希 COS 姿势与来源的回归测试。

## 1.3.90（稳定版）

- 调整 `/看看COS` 的参考图收集：保留当前消息及引用图片，但不再自动继承历史上下文图片。
- 将 COS 随机套装池整理为 13 套结构明确的真人换装提示词，减少复杂且不稳定的服装描述。
- 增加 COS 历史图片隔离、当前消息图片保留和套装池完整性回归测试。

## 1.3.89

- 增加管理面板统一的保存配置与刷新数据入口。
- 完善页面进入和手动刷新时的数据加载。
- 修复渠道列表启用/禁用开关无法直接持久化、刷新后状态丢失的问题。

## 1.3.88

- 增加后台任务短期幂等去重与强制重生成入口，减少重复扣费。
- 增加渠道健康度记录、网络/服务端失败短暂冷却和 Dashboard 手动清除入口。
- 增加配置 schema 版本、脱敏导出、导入预览与失败回滚。
- 增加图片发送失败保留与 `/生图重发` 命令。
- 增加缓存清理预览和任务持久化可靠性改进。
- 按 Bento Studio 方案重做管理面板，并让 Flask Web 与 AstrBot 内置 Web 共用 `pages/dashboard/index.html`。

## 1.3.86

- 优化生图任务并发调度：同时并发上限按所有任务合计计算，不再锁在单条指令或单个批次内。
- 修复批量生图时的排队判断：当前还有可用并发槽位时，新指令可以立即接入。
- 修复排队提示回复时机：指令回复阶段即可提示排队状态，不再晚于生成进度提示。
- 生图同时并发上限默认从 3 调整为 10，最大支持 10 张同时请求上游。
- 更新独立 Web 面板和内嵌 Dashboard 的并发配置说明。
- 10 并发真实渠道测试通过：`gpt-image-2` 渠道 10/10 请求成功。

## 1.3.85

- 修复看看腿构图裁切限制，恢复蕾丝与吊带袜相关提示约束。

## 1.3.83

- 隔离每个模型尝试的工作线程，避免单个模型请求阻塞其他模型流程。
